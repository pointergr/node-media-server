# Ο server γράφει μέσα το jwt secret και το generate-passwords τους κωδικούς:
# είναι κατάσταση του κάθε deployment, όχι πηγαίος κώδικας. Πρότυπο: config.example.json
# Unanchored (χωρίς αρχικό /) γιατί τώρα ζουν μέσα στο apps/stream, όχι στη ρίζα.
config.json
# Ρυθμίσεις του συγκεκριμένου μηχανήματος (GPU devices κλπ), όχι πηγαίος κώδικας
docker-compose.override.yml
media
node_modules
.aider*
passwords.json
data
stats.db
stats.db-shm
stats.db-wal
.env

# Nuxt panel: build artifacts
.nuxt
.output
dist
