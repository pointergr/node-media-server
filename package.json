{
  "name": "node-media-server-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "test": "npm test --workspaces --if-present",
    "start": "npm start -w apps/stream",
    "test-stream": "npm run test-stream -w apps/stream",
    "generate-passwords": "npm run generate-passwords -w apps/stream"
  },
  "volta": {
    "node": "24.19.0"
  }
}
