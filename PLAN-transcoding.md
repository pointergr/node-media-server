# Πλάνο: transcoding σε πολλαπλές αναλύσεις (ABR), ρυθμιζόμενο από τα πακέτα

Κατάσταση: **Φάσεις 1-5 υλοποιήθηκαν (2026-08-18), η Φάση 6 (GPU) εκκρεμεί.**
Γραμμένο 2026-08-18.

Σήμερα κάθε εκπομπή βγαίνει σε μία μόνο ποιότητα: ο `app.js` σπρώχνει έναν
`ffmpeg -c copy` (remux) που γράφει ένα `index.m3u8`. Ο θεατής με κακή γραμμή
βλέπει buffering ή τίποτα — δεν υπάρχει χαμηλότερη σκάλα να πέσει.

Ο στόχος είναι **ABR**: master playlist με πολλά variants, με το ladder να είναι
ιδιότητα του **πλάνου** (`Plan.ladder`), δηλαδή κάτι που πουλιέται. Και, ως
δεύτερη φάση, ο encoder να μπορεί να είναι GPU χωρίς να αλλάξει τίποτα άλλο.

## Αποφάσεις που έχουν ήδη παρθεί

**1. Ένας ffmpeg ανά εκπομπή, όχι ένας ανά rendition.**
Ο ίδιος ffmpeg που τρέχει ήδη, με `-filter_complex split` + πολλά `-map` +
`-var_stream_map`, βγάζει master και N variants. Ένα spawn, ένα `exit`, ένα
respawn — όλη η μηχανική του `app.js` (hlsJobs ανά `streamPath`, RESPAWN_MAX,
shutdown) μένει ως έχει. Πολλές διεργασίες θα σήμαιναν N φορές αποκωδικοποίηση
της ίδιας πηγής και N ζωές να συγχρονίσεις.

**2. Τα variants είναι επίπεδα αρχεία στον φάκελο του stream, όχι υποφάκελοι.**
Το `stats.js#trackHls` βγάζει το stream από `p.slice(0, p.lastIndexOf("/"))`.
Υποφάκελος `v0/` θα έδινε stream `/live/x/v0` και θα έσπαγε μονομιάς `clientOf`,
`overLimit`, τα samples και το panel. Άρα: `index.m3u8` (master), `v720.m3u8`,
`v480.m3u8`, segments `<prefix>-720-3.ts`, όλα δίπλα-δίπλα.

**3. Η κορυφή του ladder είναι πάντα η πηγή σε `copy`.**
Το ladder περιγράφει **μόνο** τα σκαλοπάτια από κάτω. Το re-encode της κορυφής
θα διπλασίαζε το κόστος για μηδενικό κέρδος ποιότητας. Τίμημα: τα segments του
copy variant σπάνε στα keyframes του OBS, ενώ τα encoded είναι κλειδωμένα στα 2s.
Αν ο πελάτης δεν έχει keyframe interval 2s, το switching τρίζει — το
τεκμηριώνουμε στο README, δεν το λύνουμε με κώδικα.

**4. Ποτέ upscale.** Rendition με ύψος ≥ της πηγής (`session.videoHeight`)
παραλείπεται. Πηγή 720p με ladder `720,480` = ένα encode, όχι δύο.
Το ύψος όμως δεν υπάρχει τη στιγμή του `postPublish` — γράφεται όταν φτάσουν τα
metadata (`broadcast_server.js:200` vs `:159`). Οπότε τα streams **με ladder**
περιμένουν το ύψος πριν σηκωθεί ο ffmpeg (100ms tick, λήξη 2s)· τα υπόλοιπα
σηκώνονται ακαριαία όπως πάντα. Χωρίς αυτό το φίλτρο δεν έκοβε ποτέ τίποτα.

**5. Το ladder διαβάζεται στο `postPublish` και μένει σταθερό όσο ζει η εκπομπή.**
Αλλαγή πλάνου εν ώρα εκπομπής δεν αλλάζει τίποτα μέχρι το επόμενο publish. Η
εναλλακτική (kill + respawn στο `sample()`) κόβει την εικόνα για 2-3s για μια
αλλαγή που γίνεται μία φορά στον μήνα. Να μη συγχέεται με την **ανάκληση**, που
παραμένει άμεση (≤10s) — εκείνη αφορά ασφάλεια, αυτό εδώ ποιότητα.

**6. Ο encoder είναι ρύθμιση του server (`config.hls.encoder`), το ladder του πλάνου.**
Το πλάνο λέει *τι πούλησες*, ο server *με τι το βγάζει*. Και τα δύο συναντιούνται
μόνα τους: το `Plan.serverId` ήδη ορίζει πού πέφτουν οι νέες συνδρομές, οπότε ένα
πλάνο με ladder που δείχνει σε μηχάνημα με GPU δρομολογείται σωστά χωρίς καμία
νέα έννοια. Οι παλιές συνδρομές μένουν όπου είναι, όπως πάντα.

**7. Πλαφόν renditions ανά server** (`config.hls.maxRenditions`), ανεξάρτητο από
το πλάνο. Το πλάνο είναι εμπορική υπόσχεση· το πλαφόν είναι το τι σηκώνει το
μηχάνημα πριν λιώσει. Χωρίς αυτό, μια αλλαγή στον κατάλογο ρίχνει έναν server.

**8. `-c:a copy` σε όλα τα variants.** Το AAC της πηγής περνάει αυτούσιο σε κάθε
σκαλοπάτι: μηδενικό κόστος, καμία απώλεια. Ξεχωριστό audio rendition (mono 64k
για 240p) δεν μπαίνει τώρα.

## Το ladder

`Plan.ladder`: csv από ύψη, π.χ. `"720,480"`. `null` ή κενό = **σημερινή
συμπεριφορά ακριβώς** (σκέτο copy, ένα playlist, κανένα master) — αυτός είναι και
ο δρόμος αναδίπλωσης.

Τα bitrates δεν είναι ρυθμιζόμενα ανά πλάνο: σταθερός πίνακας ανά ύψος.

| ύψος | video bitrate | maxrate / bufsize |
|---|---|---|
| 1080 | 5000k | 5500k / 10000k |
| 720 | 2800k | 3000k / 6000k |
| 480 | 1200k | 1400k / 2800k |
| 360 | 700k | 800k / 1600k |
| 240 | 400k | 450k / 900k |

## Σκελετός των args

Πηγή 1080p, ladder `720,480`:

```
-i rtmp://127.0.0.1:1935/live/x
-filter_complex "[0:v]split=2[a][b];[a]scale=-2:'min(720,ih)'[v720];[b]scale=-2:'min(480,ih)'[v480]"
-map 0:v -c:v:0 copy -bsf:v:0 h264_mp4toannexb,dump_extra
-map "[v720]" -c:v:1 libx264 -preset veryfast -b:v:1 2800k -maxrate:v:1 3000k -bufsize:v:1 6000k
-map "[v480]" -c:v:2 libx264 -preset veryfast -b:v:2 1200k -maxrate:v:2 1400k -bufsize:v:2 2800k
-map 0:a -map 0:a -map 0:a -c:a copy
-sc_threshold 0 -force_key_frames "expr:gte(t,n_forced*2)"
-f hls -hls_time 2 -hls_list_size 6 -hls_flags delete_segments+temp_file
-var_stream_map "v:0,a:0,name:src v:1,a:1,name:720 v:2,a:2,name:480"
-master_pl_name index.m3u8
-hls_segment_filename "<dir>/<prefix>-%v-%d.ts" "<dir>/v%v.m3u8"
```

**Keyframes σε χρόνο, όχι σε καρέ.** Χωρίς `-g`: το fps της πηγής το ορίζει ο
πελάτης, οπότε ένα `-g 50` δίπλα στο `force_key_frames` των 2s θα έβαζε δεύτερο
IDR στο 1,67s κάθε πηγής των 30fps. Για τον ίδιο λόγο το scale γράφεται
`min(h,ih)`: το `videoHeight` μπορεί να μην έχει φτάσει ακόμα στο `postPublish`,
και η εγγύηση «ποτέ upscale» δεν επιτρέπεται να κρέμεται από τον συγχρονισμό.

**`-bsf:v:0`, όχι `-bsf:v`.** Το σημερινό `h264_mp4toannexb,dump_extra` αφορά
μόνο το copy stream — στα encoded ο encoder βγάζει ήδη annex-b με δικό του
extradata, και το φίλτρο εκεί είναι στην καλύτερη περίπτωση περιττό.

## Τι σπάει αλλού και πρέπει να διορθωθεί

Δεν είναι προαιρετικά — χωρίς αυτά το ABR βγαίνει λάθος.

**`r2.js` ξέρει ένα playlist.** Νέος κανόνας: ο ffmpeg γράφει σε υποφάκελο `ff/`
με τα **τελικά** ονόματα (`ff/index.m3u8`, `ff/v720.m3u8`, segments), και το
`r2.js` δημοσιεύει τα ίδια ονόματα στον φάκελο του stream αφού ανέβουν τα
segments. Το master αντιγράφεται αυτούσιο (δεν αλλάζει ποτέ μέσα στην εκπομπή και
δείχνει σε **σχετικά** ονόματα variants, που μένουν στο origin — εκεί μετράμε
θεατές· το `-hls_base_url` αγγίζει μόνο τα segments *μέσα* στα variants). Το
master δημοσιεύεται **μετά** το πρώτο variant, αλλιώς οι πρώτοι 2-4s δίνουν 404
σε variant που το master υπόσχεται. Ο κανόνας αντικαθιστά και το σημερινό ειδικό
`ff.m3u8` → `index.m3u8`: ένας κανόνας αντί για δύο περιπτώσεις.

**`app.js#donePublish` σβήνει μόνο το `index.m3u8`.** Με ABR πρέπει να σβήνει και
τα `v*.m3u8`: ο player που ήδη παίζει δεν ξαναζητάει master, οπότε θα
ξαναπαίζει το τελευταίο variant σε βρόχο νομίζοντας ότι είναι live.

**`stats.js#addR2Out` θα υπερεκτιμούσε ×N.** Πολλαπλασιάζει bytes segment ×
θεατές του stream· με τρία variants κάθε segment μετριέται σαν να το κατέβασαν
όλοι. Διόρθωση: δεύτερος χάρτης `stream → variant → Set(θεατές)`, που τον τρέφουν
τα requests στα `v*.m3u8`, και το `addR2Out` πολλαπλασιάζει με τους θεατές **του
variant του segment** (το όνομα το λέει ήδη: `<prefix>-720-3.ts`). Το `hlsSeen`
και το `overLimit` μένουν ανέγγιχτα — ένας θεατής που αλλάζει σκαλοπάτι είναι
ένας θεατής, με το ίδιο cookie στον ίδιο φάκελο. Χωρίς R2 δεν αλλάζει τίποτα: τα
bytes μετριούνται από τα content-length των πραγματικών requests.

## Φάσεις

Κάθε φάση αφήνει τον server λειτουργικό. Χωρίς `ladder` στο πλάνο, τίποτα από
όλα αυτά δεν ενεργοποιείται και η συμπεριφορά μένει η σημερινή.

**Φάση 1 — args από ladder (`apps/stream`).**
Εξαγωγή του χτισίματος των args σε καθαρή συνάρτηση `ffmpegArgs({ dir,
streamPath, prefix, ladder, srcHeight, r2, encoder })` και νέο `test-ladder.js`:
ladder κενό ⇒ **ακριβώς** τα σημερινά args, ύψος ≥ πηγής ⇒ παραλείπεται, `-bsf`
μόνο στο `:0`, πλαφόν renditions. Καθαρή συνάρτηση = test χωρίς να τρέξει ποτέ
ffmpeg.

**Φάση 2 — `clients.json` και ανάγνωση του ladder (`apps/stream`).**
`ladderOf(path)` στο `config.js` πάνω στο υπάρχον `clientOf`, ανεκτικό σε παλιό
σχήμα (χωρίς πεδίο ⇒ κενό ladder). Το `app.js` το περνάει στο `ffmpegArgs`.

**Φάση 3 — R2 πολλαπλών playlists (`apps/stream`).**
Ο κανόνας `ff/` παραπάνω, καθαρισμός των variants στο `donePublish`, και το
`addR2Out` ανά variant. Επέκταση του `test-r2.js`.

**Φάση 4 — πλάνα (`apps/api`).**
`Plan.ladder String?` στο schema, validation στο `plans.controller.ts` (ύψη από
τον πίνακα, φθίνουσα σειρά, χωρίς διπλά), και το `sync.controller.ts` να στέλνει
`ladder` σε κάθε εγγραφή. Tests στο `dist/test/*.js` όπως τα υπόλοιπα.

**Φάση 5 — panel.**
Πεδίο ladder στη φόρμα πλάνου (`/admin/plans`), ένδειξη στη λίστα ζωντανών. Η ένδειξη
δείχνει τα σκαλοπάτια που **όντως** κωδικοποιεί ο ffmpeg (`stats.js#snapshot` ⇒ `ladder`,
τροφοδοτούμενο από τα jobs του `app.js`), όχι το ladder του πακέτου: σε πηγή 480p ένα
πακέτο «720+480» δεν παράγει τίποτα, και η ένδειξη πρέπει να το λέει.
Ο πελάτης δεν αλλάζει τίποτα: το URL αναπαραγωγής παραμένει
`https://<host><path>/index.m3u8`, απλώς είναι πια master.

**Φάση 6 — GPU (μόνο όταν το ζητήσει η ζήτηση).**
`config.hls.encoder: "x264" | "qsv" | "nvenc"`: αλλάζουν μόνο τα codec args μέσα
στο `ffmpegArgs`, το υπόλοιπο pipeline είναι ίδιο. Ένα μηχάνημα με GPU μπαίνει ως
κανονικός server στο panel και τα πλάνα με ladder δείχνουν εκεί.

## Κόστος — γιατί το ABR είναι άλλο προϊόν

Σημερινό remux: ~1-2% ενός πυρήνα. Software x264 `veryfast`, πηγή 1080p30:

| rendition | CPU |
|---|---|
| src (copy) | ~0 |
| → 720p | 0.8 – 1.2 πυρήνες |
| → 480p | 0.3 – 0.5 |
| → 360p | 0.2 – 0.3 |

Δηλαδή «src + 720 + 480» ≈ **1.2 – 1.7 πυρήνες ανά εκπομπή**: σε 8πύρηνο VPS
3-4 ταυτόχρονες εκπομπές, εκεί που σήμερα το ίδιο μηχάνημα σηκώνει δεκάδες.

Επιλογές encoder (1080p → 3 renditions, ανά μηχάνημα, τάξη μεγέθους):

| | ταυτόχρονες | κόστος/μήνα | παγίδες |
|---|---|---|---|
| x264 veryfast | 3-4 σε 8 πυρήνες | ήδη πληρωμένο | καμία |
| Intel QSV / VA-API | ~10-15 | ~€45 (dedicated με iGPU) | `/dev/dri` passthrough, intel-media-driver στο image· χωρίς όριο sessions |
| NVIDIA NVENC | 25+ | ~€180+ | nvidia-container-toolkit, GPU στο compose, ffmpeg με `scale_cuda` (το ffmpeg του Debian δεν το έχει σίγουρα)· consumer κάρτες έχουν όριο ταυτόχρονων sessions |

Ποιότητα: NVENC από Turing και πάνω, και QSV από Xe/Arc, είναι περίπου x264
`fast`/`medium` στο ίδιο bitrate — δηλαδή **καλύτερα** από το `veryfast` που θα
έβαζες ούτως ή άλλως για live. Με GPU δεν θυσιάζεις εικόνα, θυσιάζεις ευκολία
deployment.

Γι' αυτό η σειρά είναι software πρώτα: GPU πριν υπάρξει ένας πελάτης με ladder
είναι €180/μήνα για να μάθεις αν πουλάει το προϊόν.

## Εκτός πλάνου, σκόπιμα

- **Custom bitrates ανά πλάνο** — σταθερός πίνακας ανά ύψος· μπαίνουν όταν τα
  ζητήσει κάποιος ονομαστικά.
- **Audio-only rendition** για πολύ κακές γραμμές.
- **LL-HLS / DVR** — άλλο μέγεθος δουλειάς, άλλο προϊόν.
- **Transcode-on-demand** (encode μόνο όσο υπάρχει θεατής) — μπερδεύει στατιστικά
  και όρια για να γλιτώσει CPU σε streams που ούτως ή άλλως δεν βλέπει κανείς.
- **Re-encode της κορυφής** για τέλεια ευθυγράμμιση segments — διπλάσιο κόστος
  για ένα πρόβλημα που λύνεται με μια γραμμή τεκμηρίωσης για το OBS.
